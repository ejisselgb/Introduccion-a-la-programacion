posX = 0  # Posicion en X
posY = 0  # Posicion en Y
startGame = True
sizeX = 1024
sizeY = 768


while(startGame):
    pasosPersonaje = input("Ingrese cuantos pasos desea mover al personaje: ")
    orientacionPersonaje = input(
        "Indique la orientación del personaje, arriba, abajo, izquierda, derecha: ")
    if(orientacionPersonaje == "arriba"):

    elif(orientacionPersonaje == "abajo"):
    elif(orientacionPersonaje == "izquierda"):
    elif(orientacionPersonaje == "derecha"):


def suma(value1, value2, limite):
    resultado = value1 + value2
    if(resultado >= 0 and resultado <= limite):
        return resultado
    else:
        return


def resta(value1, value2):
    return value1 - value2
